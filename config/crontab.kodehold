# ── KodeHold Loop Engineering — Auto-generated crontab ──
# Generated by: workspace.py cron install
# Manual edits will be overwritten on next install

# ── bob ──
# daily-triage for bob — 8 * * * *
8 * * * * cd /home/kiffer/project/bob && START_TIME=$(date +%s) && opencode run "Run loop-triage. Read STATE.md first. Update High Priority and Watch List. No auto-fix in week one." --agent loop-triage 2>&1 | tee /tmp/loop-bob.log && END_TIME=$(date +%s) && DURATION=$((END_TIME - START_TIME)) && python3 /home/kiffer/project/kodehold/scripts/discord-notify.py bob daily-triage "Loop completed" $DURATION /tmp/loop-bob.log

# ── deepresearch ──
# daily-triage for deepresearch — 9 * * * *
9 * * * * cd /home/kiffer/project/kodehold/workspaces/deepresearch && START_TIME=$(date +%s) && opencode run "Run loop-triage. Read STATE.md first. Update High Priority and Watch List. No auto-fix in week one." --agent loop-triage 2>&1 | tee /tmp/loop-deepresearch.log && END_TIME=$(date +%s) && DURATION=$((END_TIME - START_TIME)) && python3 /home/kiffer/project/kodehold/scripts/discord-notify.py deepresearch daily-triage "Loop completed" $DURATION /tmp/loop-deepresearch.log

# ── krypto-agent ──
# daily-triage for krypto-agent — 10 * * * *
10 * * * * cd /home/kiffer/project/krypto-agent && START_TIME=$(date +%s) && opencode run "Run loop-triage. Read STATE.md first. Update High Priority and Watch List. No auto-fix in week one." --agent loop-triage 2>&1 | tee /tmp/loop-krypto-agent.log && END_TIME=$(date +%s) && DURATION=$((END_TIME - START_TIME)) && python3 /home/kiffer/project/kodehold/scripts/discord-notify.py krypto-agent daily-triage "Loop completed" $DURATION /tmp/loop-krypto-agent.log

# ── media-health-dashboard ──
# daily-triage for media-health-dashboard — 11 * * * *
11 * * * * cd /home/kiffer/project/media-health-dashboard && START_TIME=$(date +%s) && opencode run "Run loop-triage. Read STATE.md first. Update High Priority and Watch List. No auto-fix in week one." --agent loop-triage 2>&1 | tee /tmp/loop-media-health-dashboard.log && END_TIME=$(date +%s) && DURATION=$((END_TIME - START_TIME)) && python3 /home/kiffer/project/kodehold/scripts/discord-notify.py media-health-dashboard daily-triage "Loop completed" $DURATION /tmp/loop-media-health-dashboard.log

# ── pai-model-router ──
# daily-triage for pai-model-router — 8 * * * *
8 * * * * cd /home/kiffer/project/pai-model-router && START_TIME=$(date +%s) && opencode run "Run loop-triage. Read STATE.md first. Update High Priority and Watch List. No auto-fix in week one." --agent loop-triage 2>&1 | tee /tmp/loop-pai-model-router.log && END_TIME=$(date +%s) && DURATION=$((END_TIME - START_TIME)) && python3 /home/kiffer/project/kodehold/scripts/discord-notify.py pai-model-router daily-triage "Loop completed" $DURATION /tmp/loop-pai-model-router.log

# ── radarr-lang-router ──
# daily-triage for radarr-lang-router — 9 * * * *
9 * * * * cd /home/kiffer/project/radarr-lang-router && START_TIME=$(date +%s) && opencode run "Run loop-triage. Read STATE.md first. Update High Priority and Watch List. No auto-fix in week one." --agent loop-triage 2>&1 | tee /tmp/loop-radarr-lang-router.log && END_TIME=$(date +%s) && DURATION=$((END_TIME - START_TIME)) && python3 /home/kiffer/project/kodehold/scripts/discord-notify.py radarr-lang-router daily-triage "Loop completed" $DURATION /tmp/loop-radarr-lang-router.log
