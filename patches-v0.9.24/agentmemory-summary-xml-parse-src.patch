# Patch: agentmemory summary XML parsing fix — src-B8J9Exum.mjs
#
# CRITICAL: src-B8J9Exum.mjs is the ACTUAL runtime file loaded by
# cli.mjs. The sibling index.mjs is dead code. Both files must be
# patched (see agentmemory-summary-xml-parse.patch for index.mjs).
#
# Applied: 2026-06-02 15:16
# Backups: src-B8J9Exum.mjs.bak, src-B8J9Exum.mjs.orig
#
# Three changes:
#   1. Strip markdown code fences (```xml/```) from parseSummaryXml input
#   2. Extract root XML element from surrounding text before regex parsing
#   3. Retry loop (2 attempts) on the final summarize call in
#      registerSummarizeFunction
#
# Apply:
#   sudo patch -p1 -d /usr/local/lib/node_modules/@agentmemory/agentmemory/dist < patches/agentmemory-summary-xml-parse-src.patch
#   systemctl --user restart agentmemory
#
# Rollback:
#   sudo patch -p1 -R -d /usr/local/lib/node_modules/@agentmemory/agentmemory/dist < patches/agentmemory-summary-xml-parse-src.patch
#   systemctl --user restart agentmemory

--- a/src-B8J9Exum.mjs
+++ b/src-B8J9Exum.mjs
@@ -4679,6 +4683,11 @@
 	};
 }
 function parseSummaryXml(xml, sessionId, project, obsCount) {
+	// Strip markdown code fences if present (```xml ... ``` or ``` ... ```)
+	xml = xml.replace(/```xml\n?/gi, "").replace(/```\n?/g, "").trim();
+	// If extra text exists before/after the XML root element, extract just the XML
+	const rootMatch = xml.match(/(<[a-zA-Z_][a-zA-Z0-9_-]*>[\s\S]*<\/[a-zA-Z_][a-zA-Z0-9_-]*>)/);
+	if (rootMatch) xml = rootMatch[1].trim();
 	const title = getXmlTag(xml, "title");
 	if (!title) return null;
 	return {
@@ -4726,7 +4735,7 @@
 			};
 		}
 		try {
-			const { response, mode, chunks } = await produceSummaryXml(provider, compressed, sessionId, session.project);
+			let { response, mode, chunks } = await produceSummaryXml(provider, compressed, sessionId, session.project);
 			if (!response || !response.trim()) {
 				const latencyMs = Date.now() - startMs;
 				if (metricsStore) await metricsStore.record("mem::summarize", latencyMs, false);
@@ -4742,16 +4751,28 @@
 					error: "empty_provider_response"
 				};
 			}
-			const summary = parseSummaryXml(response, sessionId, session.project, compressed.length);
-			if (!summary) {
-				const latencyMs = Date.now() - startMs;
-				if (metricsStore) await metricsStore.record("mem::summarize", latencyMs, false);
-				logger.warn("Failed to parse summary XML", { sessionId });
-				return {
-					success: false,
-					error: "parse_failed"
-				};
-			}
+		let summary = null;
+		for (let attempt = 1; attempt <= 2; attempt++) {
+			summary = parseSummaryXml(response, sessionId, session.project, compressed.length);
+			if (summary) break;
+			logger.warn("Failed to parse summary XML", { sessionId, attempt });
+			if (attempt === 1) {
+				const retryResult = await produceSummaryXml(provider, compressed, sessionId, session.project);
+				response = retryResult.response;
+				mode = retryResult.mode;
+				chunks = retryResult.chunks;
+				if (!response || !response.trim()) break;
+			}
+		}
+		if (!summary) {
+			const latencyMs = Date.now() - startMs;
+			if (metricsStore) await metricsStore.record("mem::summarize", latencyMs, false);
+			logger.warn("Failed to parse summary XML after retry", { sessionId });
+			return {
+				success: false,
+				error: "parse_failed"
+			};
+		}
 			const summaryForValidation = {
 				title: summary.title,
 				narrative: summary.narrative,
