--- a/src-fQOMXeCp.mjs
+++ b/src-fQOMXeCp.mjs
@@ -7439,6 +7439,8 @@
 		const type = attrs["type"];
 		const name = attrs["name"];
 		if (!type || !name) return;
+		const VALID_TYPES = /* @__PURE__ */ new Set(["file", "function", "concept", "error", "decision", "pattern", "library", "person"]);
+		if (!VALID_TYPES.has(type)) return;
 		const properties = {};
 		const propRegex = /<property\s+key="([^"]+)">([^<]*)<\/property>/g;
 		let propMatch;
